{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "C: build current + linked_list and run",
            "command": "bash",
            "args": [
                "-c",
                "mkdir -p output && gcc -g -I. ${file} linked_list/single_linked_list.c -o output/${fileBasenameNoExtension} && ./output/${fileBasenameNoExtension}"
            ],
            "options": { "cwd": "${workspaceFolder}" },
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": true,
                "panel": "shared",
                "clear": true
            },
            "group": { "kind": "build", "isDefault": true },
            "problemMatcher": ["$gcc"]
        }
    ]
}
